from sklearn.metrics import classification_report
y_pred = model.predict(X)  # -1 for anomaly, 1 for normal
y_pred_binary = (y_pred == -1).astype(int)  # 1 for attack
print(classification_report(y_true, y_pred_binary))